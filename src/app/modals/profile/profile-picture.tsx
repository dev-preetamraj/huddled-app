import { View } from 'react-native';
import React from 'react';
import Text from '@/components/ui/Text';

const ProfilePictureModal = () => {
  return (
    <View>
      <Text>ProfilePictureModal</Text>
    </View>
  );
};

export default ProfilePictureModal;
